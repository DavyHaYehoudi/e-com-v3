import { Badge } from "@/components/ui/badge";

const StatAllYearBadge = () => {
  return (
    <Badge
      className="bg-slate-900 text-white text-xs font-bold px-2 py-1 rounded-full italic"
      variant="outline"
    >
      Toutes années
    </Badge>
  );
};

export default StatAllYearBadge;
