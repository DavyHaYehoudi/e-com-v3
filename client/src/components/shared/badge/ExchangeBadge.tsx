import { Badge } from "@/components/ui/badge";

const ExchangeBadge = () => {
  return (
    <Badge
      className="bg-violet-50 dark:bg-violet-900 text-xs font-bold px-2 py-1 rounded-full text-center"
      variant="outline"
    >
      Echange
    </Badge>
  );
};

export default ExchangeBadge;
