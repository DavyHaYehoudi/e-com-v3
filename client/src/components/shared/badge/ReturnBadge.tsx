import { Badge } from "@/components/ui/badge";

const ReturnBadge = () => {
  return (
    <Badge
      className="bg-amber-50 dark:bg-amber-900 text-xs font-bold px-2 py-1 rounded-full text-center"
      variant="outline"
    >
      Retour
    </Badge>
  );
};

export default ReturnBadge;
